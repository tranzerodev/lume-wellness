/*
 * HeroSection Messages
 *
 * This contains all the text for the HeroSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeroSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeroSection component!',
  },
});
