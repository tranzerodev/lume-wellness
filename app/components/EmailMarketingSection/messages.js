/*
 * EmailMarketingSection Messages
 *
 * This contains all the text for the EmailMarketingSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmailMarketingSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailMarketingSection component!',
  },
});
