/*
 * GiftCetificate Messages
 *
 * This contains all the text for the GiftCetificate component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GiftCetificate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GiftCetificate component!',
  },
});
