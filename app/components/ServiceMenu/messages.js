/*
 * ServiceMenu Messages
 *
 * This contains all the text for the ServiceMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ServiceMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServiceMenu component!',
  },
});
