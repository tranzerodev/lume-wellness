/*
 * MenuDropdown Messages
 *
 * This contains all the text for the MenuDropdown component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuDropdown component!',
  },
});
