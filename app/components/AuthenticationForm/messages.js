/*
 * AuthenticationForm Messages
 *
 * This contains all the text for the AuthenticationForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AuthenticationForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthenticationForm component!',
  },
});
