/*
 * VerticalDetail Messages
 *
 * This contains all the text for the VerticalDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VerticalDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerticalDetail component!',
  },
});
