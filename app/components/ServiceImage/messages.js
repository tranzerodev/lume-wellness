/*
 * ServiceImage Messages
 *
 * This contains all the text for the ServiceImage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ServiceImage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServiceImage component!',
  },
});
