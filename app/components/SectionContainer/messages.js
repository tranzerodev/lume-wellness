/*
 * SectionContainer Messages
 *
 * This contains all the text for the SectionContainer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SectionContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SectionContainer component!',
  },
});
