/*
 * TeamSlider Messages
 *
 * This contains all the text for the TeamSlider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TeamSlider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeamSlider component!',
  },
});
