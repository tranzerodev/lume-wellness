/*
 * BookingLink Messages
 *
 * This contains all the text for the BookingLink component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookingLink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookingLink component!',
  },
});
