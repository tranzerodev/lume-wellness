/*
 * InstagramPic Messages
 *
 * This contains all the text for the InstagramPic component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InstagramPic';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InstagramPic component!',
  },
});
