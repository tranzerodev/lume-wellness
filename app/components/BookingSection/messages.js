/*
 * BookingSection Messages
 *
 * This contains all the text for the BookingSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookingSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookingSection component!',
  },
});
