/*
 * ServiceQuestion Messages
 *
 * This contains all the text for the ServiceQuestion component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ServiceQuestion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServiceQuestion component!',
  },
});
