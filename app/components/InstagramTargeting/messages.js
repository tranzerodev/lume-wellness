/*
 * InstagramTargeting Messages
 *
 * This contains all the text for the InstagramTargeting component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InstagramTargeting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InstagramTargeting component!',
  },
});
