/*
 * ServiceCard Messages
 *
 * This contains all the text for the ServiceCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ServiceCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServiceCard component!',
  },
});
