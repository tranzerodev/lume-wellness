/*
 * InstagramSlider Messages
 *
 * This contains all the text for the InstagramSlider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InstagramSlider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InstagramSlider component!',
  },
});
