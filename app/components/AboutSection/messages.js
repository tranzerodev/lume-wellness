/*
 * AboutSection Messages
 *
 * This contains all the text for the AboutSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AboutSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AboutSection component!',
  },
});
