/*
 * ServiceDetail Messages
 *
 * This contains all the text for the ServiceDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ServiceDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServiceDetail component!',
  },
});
