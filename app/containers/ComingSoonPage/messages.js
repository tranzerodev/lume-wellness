/*
 * ComingSoonPage Messages
 *
 * This contains all the text for the ComingSoonPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComingSoonPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ComingSoonPage container!',
  },
});
