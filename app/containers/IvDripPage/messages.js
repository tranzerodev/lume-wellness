/*
 * IvDripPage Messages
 *
 * This contains all the text for the IvDripPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IvDripPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IvDripPage container!',
  },
});
