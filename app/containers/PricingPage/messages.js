/*
 * PricingPage Messages
 *
 * This contains all the text for the PricingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PricingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PricingPage container!',
  },
});
