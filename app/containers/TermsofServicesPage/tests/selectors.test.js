// import { selectTermsofServicesPageDomain } from '../selectors';

describe('selectTermsofServicesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
