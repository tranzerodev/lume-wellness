/*
 * CryoPage Messages
 *
 * This contains all the text for the CryoPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CryoPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CryoPage container!',
  },
});
