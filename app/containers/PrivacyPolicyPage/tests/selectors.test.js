// import { selectPrivacyPolicyPageDomain } from '../selectors';

describe('selectPrivacyPolicyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
