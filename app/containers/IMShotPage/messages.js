/*
 * MassagePage Messages
 *
 * This contains all the text for the MassagePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IMShotPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IMShotPage container!',
  },
});
