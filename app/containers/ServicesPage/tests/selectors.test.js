// import { selectServicesPageDomain } from '../selectors';

describe('selectServicesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
