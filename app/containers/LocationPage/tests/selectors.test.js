// import { selectLocationPageDomain } from '../selectors';

describe('selectLocationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
