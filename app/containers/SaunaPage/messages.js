/*
 * SaunaPage Messages
 *
 * This contains all the text for the SaunaPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SaunaPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SaunaPage container!',
  },
});
